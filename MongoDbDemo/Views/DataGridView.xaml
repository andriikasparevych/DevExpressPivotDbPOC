<UserControl x:Class="MongoDbDemo.Views.DataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MongoDbDemo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{Binding Main, Source={StaticResource Locator}}">
    <Grid>
        <DataGrid ItemsSource="{Binding Books}" AutoGenerateColumns="True" Grid.Row="0">

        </DataGrid>

    </Grid>
</UserControl>
